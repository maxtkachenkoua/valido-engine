---
title:
  en: Base64 FAQ
order: 30
status: active
---

## What is Base64URL?

Base64URL uses URL-safe characters so encoded values can appear in URLs and tokens more safely.

## Is padding required?

Padding is common in standard Base64 but some contexts omit it. The example DSL models padding as an input option.
