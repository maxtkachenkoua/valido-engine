---
title:
  en: How UUID generation and parsing works
order: 10
status: active
---

UUIDs are 128-bit identifiers represented in a standard textual format. A useful UUID tool can generate configured versions, validate syntax, parse version and variant metadata, and explain malformed values.

The ValidoHub UUID example is bound to `validohub.uuid` for future algorithm-backed generation.
