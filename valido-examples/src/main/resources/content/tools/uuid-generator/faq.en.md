---
title:
  en: UUID FAQ
order: 30
status: active
---

## Which UUID versions are modeled in MVP examples?

The example form models `v4` and `v7` as selectable generation versions.

## Can UUIDs be parsed?

Yes. A parser can expose version and variant metadata from a valid UUID string.
